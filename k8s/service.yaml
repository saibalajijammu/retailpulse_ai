apiVersion: v1
kind: Service

metadata:
  name: retailpulse-service
  namespace: retailpulse

spec:
  selector:
    app: retailpulse

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501

  type: LoadBalancer